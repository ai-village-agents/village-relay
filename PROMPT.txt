Create a single, fully self-contained mobile-first file index.html (ALL CSS and JS inline, zero external requests, no CDN, no fonts, no images — emoji only) implementing 'Prompt Relay — Fast Lane', a pass-the-phone party game for the AI Village Showcase. Read decks-source.md in this directory and copy the 20 Challenge Cards and 20 Style & Constraint Cards VERBATIM (title + quoted text) into two JS arrays.

VISUAL STYLE: warm paper-cream background (#faf6ee), dark ink text (#2a2118), coral-orange accent (#e8590c) for buttons/highlights, system font stack, large touch targets (min 48px), max-width 560px centered, generous spacing. A thin repeating diagonal-stripe 'relay track' band at the very top. Header: '🏁 Prompt Relay — Fast Lane' with subtitle 'AI Village Showcase · Station 1'. Below header a 4-step progress bar: 'Draw → Leg 1 → Leg 2 → Leg 3' plus a finish flag, current step highlighted.

STATE MACHINE (vanilla JS, state object persisted to localStorage key 'village-relay-v1' on every change; on load, if a relay is mid-flight show a banner with two buttons 'Resume relay' / 'Start fresh'):

SCREEN 0 START: Copy: 'Gather 2–4 people around one phone. You'll build one prompt in three legs, watch it drift, and pin the result to the Relay Wall of Fame.' Bullet list: '🃏 Draw a challenge', '✍️ Leg 1: write a naive one-sentence prompt', '🎭 Legs 2–3: twist it with constraint cards', '📌 Pin the start + finish side by side on the wall'. Privacy line small text: 'Everything stays on this phone unless you choose to beam your result to the Village at the end. No accounts, no names.' Fallback line small text: 'Prefer paper — or page acting up? Grab a printed Relay Worksheet at the table. Same game, zero batteries.' Button: 'Start a relay'.

SCREEN 1 DRAW: Heading 'The Draw'. Copy: 'Hand the phone to Runner 1.' Button 'Draw a Challenge Card 🃏' deals a random challenge shown as a card (bold title, quoted text) with buttons 'Draw again' and 'Lock it in'. Also a link-style button 'Using a paper Challenge Card instead? Type it here' revealing a textarea + lock-in.

SCREEN 2 LEG 1: Locked challenge card shown compact at top. Heading 'Leg 1 — The Naive Prompt'. Copy: 'Runner 1: write a raw, one-sentence prompt for this challenge. Don't overthink it — drift is the point.' Textarea. Button 'Compose the relay prompt'. Then a monospace composed-prompt block containing EXACTLY: 'You are a supportive, high-speed Prompt Relay assistant at a live event. Keep your response under 50 words, punchy and creative. Challenge: [challenge text] Prompt: [runner text]' with a big 'Copy prompt 📋' button (navigator.clipboard with execCommand fallback; on success the button text flips to 'Copied ✓' for 2s; on failure show hint 'Press and hold the text to copy'). Instruction line: 'Paste it into any AI app on a phone at the table — ChatGPT, Claude, Gemini, dealer's choice — or hand it to the station helper. Then paste the AI's reply below.' Textarea labeled 'The AI said…' + button 'Save Leg 1 & pass the phone'.

SCREEN 3 LEG 2: Running history panel at top labeled 'The relay so far' showing challenge + each completed leg's prompt/card and response in compact readable cards (this panel persists on Legs 2, 3 and the finish screen so the whole table can read every leg on one screen). Heading 'Leg 2 — The Twist'. Copy: 'Runner 2: draw a Style & Constraint card and twist the result.' Button 'Draw a Constraint Card 🎭' (random; Draw again + Lock it in; never deal the same card twice in one relay), plus the same 'using a paper card' free-text option. Composed prompt block EXACTLY: 'You are a supportive, high-speed Prompt Relay assistant at a live event. Keep your response under 50 words. Rewrite the piece below, keeping the same core idea but applying this twist: [card text] The piece: [previous AI response]' with the same copy button + paste-reply textarea + 'Save Leg 2 & pass the phone'.

SCREEN 4 LEG 3: identical mechanics to Leg 2, heading 'Leg 3 — The Final Twist', runner 3, composed prompt uses Leg 2's AI response as 'The piece' and the new card as the twist. Save button: 'Cross the finish line 🏁'.

SCREEN 5 FINISH: Heading 'The Finish Line 🏁'. A large wall-ready artifact card with cream styling and a dashed border: top label 'WHERE IT STARTED', the Leg-1 naive prompt; a divider showing the two twist card titles like '🎭 [card] → 🎭 [card]'; bottom label 'WHERE IT ENDED', the final AI response. Below it copy: 'Copy these two onto a Post-it or index card — start on top, finish below — and pin them side by side on the Relay Wall of Fame. Add a sticker dot to your favorite neighbor. Want the Village to keep yours? Drop a copy in the Leave one for the Village tray.' Buttons: 'Copy artifact text 📋' (plain text: WHERE IT STARTED: … / TWISTS: … / WHERE IT ENDED: …), 'Beam it to the Village 📡', 'Big text mode 🔍' (toggles a fullscreen overlay rendering the artifact card at ~1.5x font for holding the phone up), 'Show the full relay' (expands complete history), and 'Run another relay 🔁' (confirm dialog, clears state back to start).

BEAM BUTTON BEHAVIOR: at the top of the JS define const BEAM_URL = '' with a comment '// Google Form prefill template; placeholders {START} and {END} are replaced with encoded text. Leave empty to hide the beam button.' If BEAM_URL is empty the button is hidden entirely. If set, tapping it opens (window.open, new tab) BEAM_URL with {START} replaced by encodeURIComponent(leg-1 prompt) and {END} by encodeURIComponent(final response). Under the button, small text: 'Optional. Shares only what's on this card — the agents are watching the feed live tonight.'

GENERAL: no analytics, no other network calls, no personal data fields anywhere. Every screen has a small footer: 'AI Village Showcase · The Fold, SF · paper worksheets at the table are the fallback'. Clean, commented vanilla JS. Title tag 'Prompt Relay — Fast Lane · AI Village'. Meta viewport tag and meta description 'Pass-the-phone prompt relay game for the AI Village Showcase.'
